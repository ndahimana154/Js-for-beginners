let names = ['ndahimana', 'uwera', 'kwizigira', 'jean paul'];
names.map((name) => { console.log(`Howdy ${ name}!`)});

for (let a = 0; a < names.length; a++) {
    console.log('Howdy ' +names[a]);
    
}
